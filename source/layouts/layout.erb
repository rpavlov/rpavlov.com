<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta http-equiv='X-UA-Compatible' content='IE=edge;chrome=1' />
  <%= stylesheet_link_tag 'normalize' %>
  <%= stylesheet_link_tag 'main' %>
  <%= stylesheet_link_tag 'text' %>

  <link rel="stylesheet" href="//cdn.jsdelivr.net/gh/highlightjs/cdn-release@9.16.2/build/styles/default.min.css">
  <script src="//cdn.jsdelivr.net/gh/highlightjs/cdn-release@9.16.2/build/highlight.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', (event) => {
      document.querySelectorAll('code').forEach((block) => {
        hljs.highlightBlock(block);
      });
    });
  </script>
  <title>rpavlov</title>
</head>
<body>
  <header>
    <div class="header-avatar">
        <a class="child-avatar" href="https://www.github.com/rpavlov"></a>
    </div>
    <div class="clearfix">
          <h2 class="name"><%= link_to "rpavlov", "/" %></h2>
    </div>
  </header>
  <section id="main" role="main" class="content">
    <div class="posts-wrapper">
      <%= yield %>
    </div>
    <aside class="sidebar">
      <div class="sidebar-item">
        <h3 class="sidebar-title">Howdy stranger</h3>
        <span>
          I solve problems with and, oftentimes, without software. In the murky past I've done programming, web development, dev-ops,
          cloud infrastructure management & system administration. I enjoy thinking about and solving security problems as well.
          <br/>
          <br/>
          Shoot me an email at <%= mail_to "roumen@rpavlov.com" %>. My PGP pubkey is <%= link_to "here", "/pgp.txt" %>.
          </span>

        <br/>
        <div class="social">
          <a class="github" href="https://www.github.com/rpavlov">Twitter</a>
          <a class="soundcloud" href="https://soundcloud.com/the-sweetling">Soundcloud</a>
        </div>
      </div>
      <div class="sidebar-item borderless">
        <h3>Recent Articles</h3>
        <ol>
          <% blog.articles[0...5].each do |article| %>
          <li><a href="<%= article.url %>"><%= article.title %></a></li>
          <% end %>
        </ol>
        <ol>
          <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
          <li>
            <h5 style="margin-bottom:0;"><%= year %></h5>
              <% articles.each do |article| %>
              <br/>
              <a href="<%= article.url %>"><%= article.title %></a>
              <% end %>
          </li>
          <% end %>
        </ol>
      </div>
      <div class="sidebar-item borderless">
      <h3>Tags</h3>
      <% blog.tags.each do |tag, articles| %>
        <span class="hash-col">#<%= link_to tag, tag_path(tag) %> (<%= articles.size %>)</span>

      <% end %>
      </div>
    </aside>
  </section>
</body>
</html>
